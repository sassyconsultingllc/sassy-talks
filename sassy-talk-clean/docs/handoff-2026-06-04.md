<!--
   Copyright (c) 2026 Shane Smith / Sassy Consulting LLC. All rights reserved.
   Proprietary source. This notice is Copyright Management Information (17 U.S.C. 1202); removal or alteration prohibited.
   CodeMark: SCLLC1-sassytalkie-X2H6IH76FAYL
-->
# SassyTalkie — Session Handoff (2026-06-04)

> **Private** — do not share publicly. Internal handoff doc for the next Claude session.

## Chat reference

Continuation of a long working session focused on the SassyTalkie Android app + Cloudflare Worker relay + nascent Tauri desktop client. The prior session covered:

- v2.6.0 → v2.7.5 release cycle (12+ versions shipped to R2 with full Android NDK builds + bundled AABs)
- Cross-platform Rust core extraction (`sassy-talk-clean/core/`)
- Multiple QC sweeps via parallel sub-agents
- R2 upload bug diagnosis + worker-side alias workaround
- Endgame feature roadmap (v2.7.0/v2.7.1/v2.7.2) implemented + shipped
- Pairing convergence fix (host-side cellular_room sync, force-reconnect on QR generate/import)
- Audio playback controls (RX gain slider, speakerphone/earpiece toggle, jitter buffer preset)
- Tauri audio_cache consumer wired
- session.rs migrated to shared core

No public share link — refer the next session to this doc + the project tree under `V:\Projects\sassytalkie\sassy-talks\sassy-talk-clean\`.

---

## Where the code is right now

### Project tree (the parts that matter)

```
V:\Projects\sassytalkie\sassy-talks\sassy-talk-clean\
├── core\                          # ← v2.7.4: new shared Rust crate
│   ├── Cargo.toml
│   └── src\
│       ├── lib.rs                 # exports protocol/audio_cache/cohort_history/crypto/session
│       ├── protocol.rs            # wire opcodes (OP_PTT_START_V2=0x15, OP_WAKE=0x17, OP_REPLAY_FRAME=0x19, …)
│       ├── audio_cache.rs         # Live/Queue/Mix modes, jitter buffer, AGC mixer (1500 lines)
│       ├── cohort_history.rs      # LRU host/joiner persistence (354 lines)
│       ├── crypto.rs              # AES-256-GCM session + X25519 KEX (212 lines)
│       └── session.rs             # SessionManager (QR generate/import) — 556 lines, v2.8 migration
├── android-native\                # Rust JNI lib — depends on core via path
│   ├── Cargo.toml                 # `sassytalkie-core = { path = "../core" }`
│   └── src\
│       ├── lib.rs                 # re-exports core modules as `crate::crypto`, `crate::session`, etc.
│       ├── jni_bridge.rs          # ~2500 lines — all the JNI entry points
│       ├── audio.rs               # AudioRecord/AudioTrack via JNI + RX gain + speakerphone gate
│       ├── audio_pipeline.rs      # TX/RX threads, mic gain, squelch
│       ├── audio_effects.rs       # AEC/NS/AGC
│       ├── audio_routing.rs       # MODE_IN_COMMUNICATION + force_speaker
│       ├── codec.rs               # libopus wrapper (Android: builds via cc)
│       ├── cellular_transport.rs  # WS-relay client (relay.sassyconsultingllc.com)
│       └── …
├── tauri-desktop\src-tauri\       # Windows/macOS/Linux desktop — depends on core
│   ├── Cargo.toml                 # `sassytalkie-core = { path = "../../core" }`
│   └── src\
│       ├── lib.rs                 # RX loop now uses sassytalkie_core::audio_cache (v2.8)
│       ├── audio.rs               # cpal-based audio I/O
│       ├── codec.rs               # audiopus crate wrapper
│       ├── transport\
│       │   ├── bluetooth.rs       # ✓ local BT
│       │   ├── wifi.rs            # ✓ LAN multicast
│       │   ├── control.rs         # ✓ uses sassytalkie_core::protocol opcodes
│       │   ├── manager.rs         # transport orchestrator
│       │   └── (no cellular.rs ← THE BLOCKER, see "next task" below)
│       └── security\
│           └── crypto.rs          # parallel implementation; pending unification with core::crypto
├── android-app\app\               # Kotlin / Compose UI
│   └── src\main\java\com\sassyconsulting\sassytalkie\
│       ├── SassyTalkNative.kt     # JNI wrapper
│       ├── TranscriptionBridge.kt # speech VAD, timeline persistence
│       ├── PttCoordinator.kt      # heartbeat, liveness, peer roster (v2.7.1)
│       ├── WalkieService.kt       # foreground service, force-reconnect, network type
│       ├── ui\
│       │   ├── MainScreen.kt      # peer chip, cache strip, network badge
│       │   ├── SettingsScreen.kt  # Playback card (RX gain, speakerphone, jitter), Diagnostics
│       │   ├── TranscriptionFeedScreen.kt   # Timeline with replay
│       │   ├── QRAuthScreen.kt    # Show QR / Scan / Enter Code / My Cohorts
│       │   ├── MyCohortsTab.kt    # rejoin via stored credentials (v2.7.3)
│       │   └── AppNavigation.kt   # screen switching + force-reconnect glue
│       └── …
├── cloudflare-worker\             # PTT relay (Durable Object)
│   └── src\
│       ├── ptt-relay-worker.js    # /auth, /ws, /share, /presence, /dl/apk
│       └── ptt-relay.js           # PttRoom DO — ring buffer, FCM fan-out, alarms
├── docs\
│   ├── cross-platform-core.md     # what's in core, what's left
│   ├── endgame-2.7.x.md           # the v2.7.0/1/2 feature plan
│   ├── bitrate-guard-design.md    # deferred
│   ├── deferred-hardening.md      # deferred
│   └── handoff-2026-06-04.md      # this doc
└── scripts\
    └── ship.sh                    # one-button release: push versioned APK+AAB → flip worker env → deploy
```

External directories used:

- `V:\Projects\sassyconsultingllc-cloudflare\` — the main website worker (separate git repo) where the `/download/sassy-talk/android/sassytalkie.apk` URL is served. **`src/worker.js` has a `aliasMap` (added in v2.7.5) that resolves canonical APK/AAB paths to versioned R2 keys via `env.LATEST_ANDROID_APK` / `env.LATEST_ANDROID_AAB`**. Wrangler config `wrangler.jsonc` carries those env vars.

### Build commands that are known to work

```bash
# Android Rust (produces libsassytalkie.so for app/jniLibs)
cd V:/Projects/sassytalkie/sassy-talks/sassy-talk-clean/android-native
cargo ndk -t aarch64-linux-android -t x86_64-linux-android -o ./jniLibs build --release

# Copy fresh .so into the app
cp jniLibs/arm64-v8a/libsassytalkie.so ../android-app/app/src/main/jniLibs/arm64-v8a/
cp jniLibs/x86_64/libsassytalkie.so   ../android-app/app/src/main/jniLibs/x86_64/

# Bundle release AAB (requires keystore.properties + release.keystore)
cd ../android-app
./gradlew :app:bundleRelease

# Universal APK from AAB (for sideload testing)
java -jar V:/Projects/sassytalkie/ci-aab/bundletool.jar build-apks \
  --bundle=app/build/outputs/bundle/release/app-release.aab \
  --output=.../app-release-universal.apks \
  --mode=universal \
  --ks=app/keystore/release.keystore \
  --ks-pass="pass:tEfLKWQK97h/xyRBKR25KLVHOxCHvGCA" \
  --ks-key-alias=sassytalkie \
  --key-pass="pass:tEfLKWQK97h/xyRBKR25KLVHOxCHvGCA"

# Tauri desktop check (no full build pipeline tested — cargo check only)
cd V:/Projects/sassytalkie/sassy-talks/sassy-talk-clean/tauri-desktop/src-tauri
cargo check

# Worker deploy (sassyconsultingllc account)
cd V:/Projects/sassyconsultingllc-cloudflare
export CLOUDFLARE_ACCOUNT_ID=d5ccd0d80780f2d0ab72ab12b15244e2
npx wrangler@latest deploy

# Ship a release (versioned + canonical via worker alias)
cd V:/Projects/sassytalkie/sassy-talks/sassy-talk-clean
scripts/ship.sh 2.7.6
```

### Build commands that are known to BREAK

```bash
# Wrangler 4.60-4.98 on this Windows install silently truncates large PUTs to
# the literal canonical key. Reports "Upload complete" in ~2 s, writes 0 B.
wrangler r2 object put sassy-downloads/sassy-talk/android/sassytalkie.apk \
  --file=universal.apk --remote
# ↑ DON'T. Always push to versioned key + use the worker alias.
```

## What v2.7.5 ships

| Feature | Where |
|---|---|
| RX volume slider (0.25x–4.0x) | Settings → Playback → RX Volume |
| Speakerphone ↔ Earpiece toggle | Settings → Playback → Loud Speaker / Earpiece |
| Jitter buffer preset (Low-Latency / Balanced / Smooth) | Settings → Playback → Buffer |
| Group Mix mode toggle | Settings → Group Voice |
| Peer roster chip on MainScreen | top of MainScreen below title |
| Peer join/leave snackbar | bottom of MainScreen |
| Cache mini-strip on MainScreen | below status |
| Network type badge (WiFi/Cellular/etc.) | top of MainScreen |
| Diagnostic info sheet | Settings → Diagnostics → Show Diagnostic Info |
| Persisted timeline | filesDir/timeline_v1.json, restored at boot |
| Test Relay button | Settings → Diagnostics → Test Relay |
| Pairing-success context toast | "Joined {device} on {channel}" |
| Rejoin without re-scanning QR | MyCohorts tab → Rejoin (uses stored credentials) |
| Persistent download URLs | sassyconsultingllc.com/download/sassy-talk/android/sassytalkie.{apk,aab} |

## Known issues / deferred work

- **Tauri cellular transport** — does NOT exist. Desktop can't join Android sessions over the internet. **This is the next task.**
- **Tauri crypto API mismatch** — `tauri-desktop/src-tauri/src/security/crypto.rs` returns `(ciphertext, auth_tag)`; core returns `nonce||ct||tag`. Unify on core's API for v2.9.
- **Tauri cohort_history consumer** — desktop has no rejoin history. Pending desktop UI.
- **Codec unification** — Android: libopus via `cc`. Tauri: `audiopus_sys`. Either works, pick one.
- **iOS native** — `ios-native/` exists but not touched. Same pattern when revived.
- **AES-GCM nonce_prefix widening** (4 → 8 bytes) — wire protocol break, needs version bump + interop test. See `docs/deferred-hardening.md`.
- **Lock-order audit** — Rust `audio_cache ↔ transport ↔ audio` mutex acquisition order documentation. Not mechanical.
- **Bitrate-guard** — design only, see `docs/bitrate-guard-design.md`.
- **Server-side ring buffer replay** consumer in Android client — server has `replay_since` opcode, client never sends the request.
- **Wrangler 4.x R2 PUT bug on Windows** — see `scripts/ship.sh`; structurally routed around via worker alias. Don't waste time re-diagnosing.
- **Three pre-existing audio_cache unit-test failures** — `test_cache_live_passthrough`, `test_idle_beacon_does_not_force_queue_mode`, `test_queue_mode_recovers_to_live_while_one_speaker_continues`. Pre-date v2.7.x; not caused by recent work. Live-mode jitter buffer expectations.

## Live endpoints

| Resource | URL |
|---|---|
| Canonical APK (always-latest) | https://sassyconsultingllc.com/download/sassy-talk/android/sassytalkie.apk |
| Canonical AAB | https://sassyconsultingllc.com/download/sassy-talk/android/sassytalkie.aab |
| Versioned (v2.7.5) APK | https://sassyconsultingllc.com/download/sassy-talk/android/sassytalkie-v2.7.5.apk |
| PTT relay WebSocket | wss://relay.sassyconsultingllc.com/ws?room={sid}&token={hmac} |
| Relay auth token | https://relay.sassyconsultingllc.com/auth?room={sid} |
| Short link (QR codes) | https://relay.sassyconsultingllc.com/dl/apk → 302 → canonical APK |

## Cloudflare accounts in play

- **Sassy Consulting LLC** — id `d5ccd0d80780f2d0ab72ab12b15244e2` — owns the workers, R2 buckets, Workers KV for `/share` tokens, the relay DO
- **Corys Websites** — id `e0581207083d15d0bb47a17d17ed0862` — has a duplicate `sassy-downloads` bucket (legacy, contains v2.6.x APK from earlier ops; unused now)

Worker auth tokens are wrangler-managed OAuth; don't search for them in user home dir (auto-mode classifier blocks credential exploration).

---

# 🚀 Next-session prompt

Copy the block below verbatim into the next Claude session as the opening message. Include this entire handoff doc by reference (it's at `V:\Projects\sassytalkie\sassy-talks\sassy-talk-clean\docs\handoff-2026-06-04.md`).

---

```
You are picking up the SassyTalkie cross-platform project after a long prior
session that shipped v2.7.5 (Android) and extracted shared Rust modules into
a new `sassytalkie-core` crate. Read `V:\Projects\sassytalkie\sassy-talks\
sassy-talk-clean\docs\handoff-2026-06-04.md` FIRST — it is your authoritative
project map. Do not re-discover what's documented there.

## Your task (two phases, in this order)

### Phase 1 — 100% comprehensive codebase audit (BEFORE writing any new code)

Produce a written audit covering the ENTIRE `sassy-talk-clean/` tree plus
the sibling `V:\Projects\sassyconsultingllc-cloudflare\` worker. The audit
must include:

1. **Module-by-module inventory**: every Rust crate (`core`, `android-native`,
   `tauri-desktop/src-tauri`, `ios-native`), every Kotlin source file,
   every JavaScript worker file. For each module:
   - Public API surface (functions/types exposed to other modules)
   - Direct dependencies (what it imports)
   - Reverse dependencies (who imports it)
   - LoC count
   - Health flag: ✓ clean, ⚠ has warnings, ✗ has compile errors

2. **Wire-protocol catalogue**: every opcode in `core/src/protocol.rs`,
   plus every desktop-only opcode in `tauri-desktop/.../transport/control.rs`,
   plus every relay-level opcode in `cloudflare-worker/src/ptt-relay.js`.
   Document the wire format byte-by-byte, expected payload length, who emits
   it, who consumes it, and any mismatch between Android / desktop / relay.

3. **Build matrix**: for each crate/project, list:
   - The exact command that works
   - The exact command that DOES NOT work (with the reason) — include the
     wrangler 4.x large-PUT bug already documented
   - Required env vars / secrets
   - Cross-compilation targets exercised

4. **Test coverage map**: every `#[cfg(test)]` / `@Test` / JS test file.
   Which paths are covered, which are NOT. Highlight any production-critical
   path with zero tests.

5. **Race / lock / lifetime hazards**: walk every `Mutex`, `RwLock`,
   `Arc`, `synchronized`, and `MutableStateFlow` mutation site. Document
   lock order, whether the convention is enforced, and whether any cross-
   module path could deadlock. (The session left a `lock_order` audit item
   explicitly TODO.)

6. **Security surface inventory**: every JNI entry point in
   `android-native/src/jni_bridge.rs` — does each one use
   `unwrap_or_else(into_inner)` for poisoned-mutex safety? Every worker
   route in `ptt-relay-worker.js` — is it gated by `AUTH_SECRET`? Every
   stored secret (keystore, FCM JSON, AUTH_SECRET) — where it lives.

7. **Deferred-work tracker**: cross-reference every TODO/FIXME comment,
   every "v2.8" / "v2.9" marker in code, every item in
   `docs/deferred-hardening.md`, `docs/bitrate-guard-design.md`,
   `docs/cross-platform-core.md`. Produce a single prioritized backlog.

8. **Dead code report**: any module/function/type with zero reverse
   references after the recent refactor (e.g., `lastHistoryId` JNI export
   is now unused but still exported).

The audit goes to `docs/audit-2026-06-XX.md`. Use parallel sub-agents
(superpowers:dispatching-parallel-agents skill) — at LEAST 5 agents covering
distinct slices (core/Rust, android Kotlin, android JNI, tauri,
cloudflare-worker) — and AGGREGATE THEIR FINDINGS into one coherent report.
Don't relay raw agent output; synthesize.

### Phase 2 — Build the Tauri cellular relay transport

Goal: a Windows / macOS / Linux desktop running tauri-desktop can pair with
an Android phone running v2.7.5+ via QR/share-link and exchange audio over
the Cloudflare relay at relay.sassyconsultingllc.com. NO new wire-protocol
changes.

Files to create:
- `tauri-desktop/src-tauri/src/transport/cellular.rs` — WebSocket client
  using `tokio-tungstenite`. Connect URL:
  `wss://relay.sassyconsultingllc.com/ws?room={session_id}&peer={peer_id}&device={device_name}&token={hmac_token}`
  Token obtained via `https://relay.sassyconsultingllc.com/auth?room={sid}`
  Frames are length-delimited binary; OUTGOING frames are `nonce||ciphertext
  ||tag` (AES-256-GCM, key from session import via `sassytalkie_core::session`);
  INCOMING frames must be parsed via `sassytalkie_core::protocol::parse_tlv`
  for control or treated as opaque encrypted audio for the audio path.
- `tauri-desktop/src-tauri/src/transport/session_import.rs` — wraps
  `sassytalkie_core::session::SessionManager::import_session` and exposes a
  tauri command `import_session_from_qr_json(json: String) -> Result<...>`.
  Same exit conditions as Android's `nativeImportSessionFromQR` — set the
  room ID, install the crypto key, persist to local config file
  (`$APP_DATA/sessions.json`).

Files to modify:
- `tauri-desktop/src-tauri/src/transport/manager.rs` — add cellular as
  third transport variant alongside bluetooth + wifi.
- `tauri-desktop/src-tauri/src/transport/mod.rs` — `pub mod cellular;
  pub mod session_import;`
- `tauri-desktop/src-tauri/src/commands.rs` — add tauri commands
  `import_session_from_qr_json`, `import_session_from_share_link`,
  `cellular_connect`, `cellular_disconnect`, `cellular_status`.
- `tauri-desktop/src-tauri/Cargo.toml` — add deps:
  `tokio-tungstenite = "0.21"`, `futures-util = "0.3"`, plus reqwest if
  not present (for /auth GET). Pull `crate::audio` integration so the
  cellular RX channel feeds the existing `audio_cache` pipeline added in
  v2.8.
- `tauri-desktop/src/` (frontend HTML/JS) — wire a "Paste invite link" or
  "Scan QR" UI; on submit, call the new command. Display connect status.

Reference implementation: `android-native/src/cellular_transport.rs`.
The protocol is identical — opcodes via `sassytalkie_core::protocol`,
session via `sassytalkie_core::session`, encryption via
`sassytalkie_core::crypto`. The TX path is just: opus-encode → AES-GCM
encrypt → ws.send(binary). The RX path is: ws.recv(binary) → check first
byte → if TLV opcode handle as control, otherwise AES-GCM decrypt →
opus-decode → feed into audio_cache (already wired in v2.8).

## Constraints

- DO NOT change the wire protocol. Adding a new opcode is fine, but no
  format breaks. Android v2.7.5 clients must interoperate.
- DO NOT touch `core/src/crypto.rs` API. If tauri's
  `security/crypto.rs` needs to be unified, do it in a separate PR.
- DO NOT introduce a new shared mutex without documenting the lock order
  in `docs/cross-platform-core.md`.
- If you hit the wrangler R2 PUT bug, USE `scripts/ship.sh`. Don't
  re-debug.

## Verification

- `cd core && cargo test --lib` → all but the 3 pre-existing Live-jitter
  failures pass.
- `cd android-native && cargo ndk -t aarch64-linux-android build --release` → clean.
- `cd tauri-desktop/src-tauri && cargo check` → clean.
- `cd tauri-desktop && cargo tauri build` → produces a Windows installer.
- Manual: install the new desktop build on Windows, on Android run v2.7.5,
  generate a QR on Android, paste the share-link on desktop, hold PTT on
  either side, hear audio on the other side.

## Definition of done

v2.8.0 ships with all of the above. AAB to Play, EXE/MSI to R2 via
`scripts/ship.sh` (extend the script if needed for desktop artifacts).
Update the canonical worker aliases for `LATEST_WINDOWS_MSI` /
`LATEST_WINDOWS_EXE`. Update `docs/cross-platform-core.md` to mark the
tauri cellular item ✓.

Report back with:
1. Audit doc path
2. Files created / modified (with LoC)
3. Tested platforms
4. Open follow-ups
```

---

## What I'd also pass along verbally

- The user (Shane / SaS) wants direct, action-first communication. No padded preambles. No "I'd be happy to". Sas-workflow skill applies.
- "Just do it" is the default for standard ops. Ask only for genuinely-blocking decisions.
- He runs Android tests on a Samsung Galaxy S24 Ultra ("Brick 2.0", serial `R5CX820XFQN`) and historically a Zebra TC21 (`20357523021619`). Both often disconnect from ADB mid-session; reconnect requests are routine.
- The cellular relay at `relay.sassyconsultingllc.com` is in production and serving live traffic. Don't break it.
- He prefers fewer, larger commits over many small ones. v2.X.0 boundaries are good commit points.
- He has actively pushed back on the "audit forever" trap. Once an audit-derived plan is laid out, stop scanning and ship the plan. Further bugs go in v2.X.(N+1) backlog.
