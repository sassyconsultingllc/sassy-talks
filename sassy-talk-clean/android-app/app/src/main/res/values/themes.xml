<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Slate chrome to match the Tauri desktop reference + Compose Theme.kt.
         Edge-to-edge (API 35 default): no statusBarColor/navigationBarColor —
         those params are deprecated; MainActivity calls enableEdgeToEdge() and
         the Compose root insets-pads content over the window background. -->
    <style name="Theme.SassyTalk" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0F172A</item>
    </style>
</resources>
